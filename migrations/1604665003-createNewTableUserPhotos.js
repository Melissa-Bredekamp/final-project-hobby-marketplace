exports.up = async (sql) => {
  await sql`
	CREATE TABLE IF NOT EXISTS user_photos (  
		user_photo_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		user_profile_id INT UNIQUE REFERENCES users_profile (user_profile_id),	
		user_photo_title varchar(40),
		user_photo_filename varchar(40)
	);
	`;
};

exports.down = async (sql) => {
  await sql`
	DROP TABLE IF EXISTS users;
 `;
};

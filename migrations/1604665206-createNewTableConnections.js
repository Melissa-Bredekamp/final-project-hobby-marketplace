exports.up = async (sql) => {
  await sql`
	CREATE TABLE IF NOT EXISTS connections (  
		connection_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		user1_id INT UNIQUE REFERENCES users (id),	
		user2_id INT UNIQUE REFERENCES users (id)	
	);
	`;
};

exports.down = async (sql) => {
  await sql`
	DROP TABLE IF EXISTS connections;
 `;
};

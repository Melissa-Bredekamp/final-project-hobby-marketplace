exports.up = async (sql) => {
  await sql`
	CREATE TABLE IF NOT EXISTS hobby_photos (  
		hobby_photo_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		hobby_id INT UNIQUE REFERENCES hobby (hobby_id),
		hobby_photo_title varchar(40),
		hobby_photo_filename varchar(40)
	);
	`;
};

exports.down = async (sql) => {
  await sql`
	DROP TABLE IF EXISTS hobby_photos;
 `;
};

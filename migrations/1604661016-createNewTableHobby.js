exports.up = async (sql) => {
  await sql`
	CREATE TABLE IF NOT EXISTS hobby (  
		hobby_id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		user_id INT REFERENCES users (id),
		availability varchar(40),
		hobby_photo varchar(240),
		hobby_offer varchar(40),
		city varchar(40),
		about_me varchar(225)
	);
	`;
};

exports.down = async (sql) => {
  await sql`
	DROP TABLE IF EXISTS hobby;
 `;
};
